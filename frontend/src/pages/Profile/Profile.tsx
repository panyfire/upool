import React from 'react'
import { Text } from 'ui'
import { GradientBackground } from 'layouts/GradientBackground'

export const Profile = () => {
  return (
    <GradientBackground>
      HOME PAGE
      <Text text={'PROFILE PAGE'} type={'h1'} />
    </GradientBackground>
  )
}
